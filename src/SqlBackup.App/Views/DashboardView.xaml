<UserControl x:Class="SqlBackup.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <TextBlock DockPanel.Dock="Top" Text="Dashboard" Style="{StaticResource SectionHeader}" />

    <Border DockPanel.Dock="Top" BorderBrush="#FFCBD5E0" BorderThickness="1" CornerRadius="4"
            Padding="14,10" Background="#FFF6F8FA">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel>
          <StackPanel Orientation="Horizontal">
            <Ellipse Width="12" Height="12" Margin="0,0,8,0" VerticalAlignment="Center">
              <Ellipse.Style>
                <Style TargetType="Ellipse">
                  <Setter Property="Fill" Value="#FFCC4444" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding ServiceReachable}" Value="True">
                      <Setter Property="Fill" Value="#FF3AA655" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Ellipse.Style>
            </Ellipse>
            <TextBlock Text="{Binding ServiceStateText}" FontSize="15" FontWeight="SemiBold" />
          </StackPanel>
          <TextBlock Text="{Binding ServiceDetailText}" Foreground="#FF57606A" Margin="20,4,0,0" TextWrapping="Wrap" />
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Content="Start" Command="{Binding ServiceCommands.StartCommand}" Padding="10,4" Margin="4,0" />
          <Button Content="Stop" Command="{Binding ServiceCommands.StopCommand}" Padding="10,4" Margin="4,0" />
          <Button Content="Reload config" Command="{Binding ReloadServiceConfigCommand}" Padding="10,4" Margin="4,0" />
          <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="10,4" Margin="4,0" />
        </StackPanel>
      </Grid>
    </Border>

    <TextBlock DockPanel.Dock="Top" Text="Backup jobs" FontSize="15" FontWeight="SemiBold" Margin="0,16,0,8" />

    <DataGrid ItemsSource="{Binding Jobs}" AutoGenerateColumns="False" IsReadOnly="True"
              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
              CanUserAddRows="False" SelectionMode="Single">
      <DataGrid.RowStyle>
        <Style TargetType="DataGridRow">
          <Style.Triggers>
            <DataTrigger Binding="{Binding LastRunSuccess}" Value="False">
              <Setter Property="Background" Value="#FFFBE9E7" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsRunning}" Value="True">
              <Setter Property="Background" Value="#FFE3F2FD" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </DataGrid.RowStyle>
      <DataGrid.Columns>
        <DataGridTextColumn Header="Job" Binding="{Binding Name}" Width="2*" />
        <DataGridTextColumn Header="State" Binding="{Binding StateText}" Width="*" />
        <DataGridTextColumn Header="Next run" Binding="{Binding NextRunText}" Width="1.5*" />
        <DataGridTextColumn Header="Last run" Binding="{Binding LastRunText}" Width="*" />
        <DataGridTextColumn Header="Last result" Binding="{Binding LastResultText}" Width="2*" />
        <DataGridTemplateColumn Header="" Width="Auto">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Button Content="Run now" Padding="8,2" Margin="2"
                      Command="{Binding DataContext.RunJobCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                      CommandParameter="{Binding}" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</UserControl>
