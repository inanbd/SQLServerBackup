<UserControl x:Class="SqlBackup.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <TextBlock DockPanel.Dock="Top" Text="Job history" Style="{StaticResource SectionHeader}" />

    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
      <TextBlock Text="Job:" VerticalAlignment="Center" Margin="0,0,8,0" />
      <ComboBox Width="220" ItemsSource="{Binding JobFilters}" SelectedItem="{Binding SelectedFilter}" />
      <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="12,5" Margin="12,0,0,0" />
      <TextBlock Text="{Binding SourceText}" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="#FF57606A" />
    </StackPanel>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="2*" />
        <RowDefinition Height="8" />
        <RowDefinition Height="*" MinHeight="90" />
      </Grid.RowDefinitions>

      <DataGrid ItemsSource="{Binding Rows}" SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                SelectionMode="Single" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
        <DataGrid.RowStyle>
          <Style TargetType="DataGridRow">
            <Style.Triggers>
              <DataTrigger Binding="{Binding Success}" Value="False">
                <Setter Property="Background" Value="#FFFBE9E7" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
          <DataGridTextColumn Header="Started" Binding="{Binding StartedText}" Width="Auto" />
          <DataGridTextColumn Header="Job" Binding="{Binding JobName}" Width="*" />
          <DataGridTextColumn Header="Database" Binding="{Binding Database}" Width="*" />
          <DataGridTextColumn Header="Type" Binding="{Binding TypeText}" Width="Auto" />
          <DataGridTextColumn Header="Trigger" Binding="{Binding TriggerText}" Width="Auto" />
          <DataGridTextColumn Header="Result" Binding="{Binding ResultText}" Width="Auto" />
          <DataGridTextColumn Header="Duration" Binding="{Binding DurationText}" Width="Auto" />
          <DataGridTextColumn Header="Size" Binding="{Binding SizeText}" Width="Auto" />
          <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="1.6*" />
        </DataGrid.Columns>
      </DataGrid>

      <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" Background="Transparent" />

      <TextBox Grid.Row="2" Text="{Binding DetailText, Mode=OneWay}" IsReadOnly="True"
               FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto" Background="#FFF6F8FA" />
    </Grid>
  </DockPanel>
</UserControl>
