<UserControl x:Class="SqlBackup.App.Views.JobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <TextBlock DockPanel.Dock="Top" Text="Backup jobs" Style="{StaticResource SectionHeader}" />

    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
      <Button Content="Add…" Command="{Binding AddCommand}" Padding="12,5" Margin="0,0,8,0" />
      <Button Content="Edit…" Command="{Binding EditCommand}" Padding="12,5" Margin="0,0,8,0" />
      <Button Content="Delete" Command="{Binding DeleteCommand}" Padding="12,5" Margin="0,0,8,0" />
      <Button Content="Enable/Disable" Command="{Binding ToggleEnabledCommand}" Padding="12,5" Margin="0,0,8,0" />
      <Button Content="Run now" Command="{Binding RunNowCommand}" Padding="12,5" FontWeight="SemiBold" />
    </StackPanel>

    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolVis}}">
      <ProgressBar IsIndeterminate="True" Width="160" Height="14" Margin="0,0,10,0" />
      <TextBlock Text="{Binding BusyText}" VerticalAlignment="Center" />
    </StackPanel>

    <DataGrid ItemsSource="{Binding Jobs}" SelectedItem="{Binding SelectedJob, Mode=TwoWay}"
              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
              SelectionMode="Single" GridLinesVisibility="Horizontal" HeadersVisibility="Column"
              MouseDoubleClick="OnRowDoubleClick">
      <DataGrid.RowStyle>
        <Style TargetType="DataGridRow">
          <Style.Triggers>
            <DataTrigger Binding="{Binding Enabled}" Value="False">
              <Setter Property="Foreground" Value="Gray" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </DataGrid.RowStyle>
      <DataGrid.Columns>
        <DataGridCheckBoxColumn Header="On" Binding="{Binding Enabled}" Width="Auto" IsReadOnly="True" />
        <DataGridTextColumn Header="Job" Binding="{Binding Name}" Width="1.4*" />
        <DataGridTextColumn Header="Connection" Binding="{Binding ConnectionName}" Width="*" />
        <DataGridTextColumn Header="Databases" Binding="{Binding DatabasesText}" Width="1.4*" />
        <DataGridTextColumn Header="Type" Binding="{Binding TypeText}" Width="Auto" />
        <DataGridTextColumn Header="Schedule" Binding="{Binding ScheduleText}" Width="1.4*" />
        <DataGridTextColumn Header="Destination" Binding="{Binding DestinationFolder}" Width="1.6*" />
        <DataGridTextColumn Header="Storage" Binding="{Binding StorageText}" Width="1.2*" />
        <DataGridTextColumn Header="Retention" Binding="{Binding RetentionText}" Width="*" />
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</UserControl>
