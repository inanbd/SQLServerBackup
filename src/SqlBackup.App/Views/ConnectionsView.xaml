<UserControl x:Class="SqlBackup.App.Views.ConnectionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <TextBlock DockPanel.Dock="Top" Text="SQL Server connections" Style="{StaticResource SectionHeader}" />

    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
      <Button Content="Add…" Command="{Binding AddCommand}" Padding="12,5" Margin="0,0,8,0" />
      <Button Content="Edit…" Command="{Binding EditCommand}" Padding="12,5" Margin="0,0,8,0" />
      <Button Content="Delete" Command="{Binding DeleteCommand}" Padding="12,5" />
    </StackPanel>

    <DataGrid ItemsSource="{Binding Connections}" SelectedItem="{Binding SelectedConnection, Mode=TwoWay}"
              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
              SelectionMode="Single" GridLinesVisibility="Horizontal" HeadersVisibility="Column"
              MouseDoubleClick="OnRowDoubleClick">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
        <DataGridTextColumn Header="Server" Binding="{Binding Server}" Width="2*" />
        <DataGridTextColumn Header="Authentication" Binding="{Binding AuthMode}" Width="*" />
        <DataGridTextColumn Header="Login" Binding="{Binding Username}" Width="*" />
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</UserControl>
